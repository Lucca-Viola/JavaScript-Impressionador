// Declaração e Expressão de Função

let total = function soma(a, b) {
  return a + b;
};

console.log(total(5, 10)); // 15
// A função soma é uma expressão de função, pois está atribuída a uma variável.
// Isso significa que ela não pode ser chamada antes de ser definida.
